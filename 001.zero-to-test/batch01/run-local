if [ $# -lt 1 ]; then
    echo "run-script <script>"
fi
script=$1
shortscript=$(echo "$script" | cut -f 1 -d '.')
# https://hub.docker.com/_/python 
docker run -it --rm --name $shortscript -v "$PWD":/usr/src/myapp -w /usr/src/myapp python:3 python $script

